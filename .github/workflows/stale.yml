name: Mark stale issues and pull requests

on:
  schedule:
  - cron: "0 0 * * *"

jobs:
  stale:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/stale@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        stale-issue-message: 'Stale : This ISSUE has been here for quite a long time!!! plz check!'
        stale-pr-message: 'Stale  : This PR has been here for quite a long time!!! plz check!'
        stale-issue-label: 'no-issue-activity'
        stale-pr-label: 'no-pr-activity'
